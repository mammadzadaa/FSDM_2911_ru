<UserControl x:Class="Contact_App.View.ContactListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Contact_App.View"
             xmlns:controls="clr-namespace:Contact_App.Controls"
             mc:Ignorable="d" 
             d:Background="White"
             d:DesignHeight="450" d:DesignWidth="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <TextBox></TextBox>
        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1"/>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Contacts}" 
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name"/>
                <DataGridTextColumn Binding="{Binding Number}" Header="Phone"/>
                <DataGridCheckBoxColumn Binding="{Binding Favorite}" Header="Is Favorite"/>
            </DataGrid.Columns>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <TextBlock Margin="10" Text="{Binding Position}"/>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
     
    </Grid>
</UserControl>
